# Q&M AI Enquiry & Enrollment System — backend (Python 3.13)
# Agentic core
crewai>=0.86.0
openai>=1.40.0

# Web / API
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0

# Database (psycopg 3 + connection pool)
psycopg[binary,pool]>=3.1.18

# Config / validation
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0

# PDF + QR (invoice / receipt / credit note engine, PayNow QR)
reportlab>=4.1.0
qrcode[pil]>=7.4.2

# Scheduling (Module A follow-ups, Module C nightly report)
APScheduler>=3.10.0
